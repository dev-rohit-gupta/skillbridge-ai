import {useMutation, useQuery} from "@tanstack/react-query";
import {FileUp, RefreshCw} from "lucide-react";
import {useRef, useState} from "react";
import {Link} from "react-router-dom";
import {api, errorMessage} from "../lib/api";
import {queryClient} from "../lib/query-client";
type Resume = {id: string; displayName: string; originalFilename: string; status: string; isActive: boolean; contentRevision: number; confirmedRevision: number; processingError?: string};
export function ResumesPage() {const fileRef = useRef<HTMLInputElement>(null); const [displayName, setDisplayName] = useState(""); const resumes = useQuery({queryKey: ["resumes"], queryFn: async () => (await api.get<{data: Resume[]}>("/resumes")).data.data, refetchInterval: (query) => query.state.data?.some((item) => ["UPLOADED","PROCESSING"].includes(item.status)) ? 2000 : false}); const upload = useMutation({mutationFn: async () => {const file = fileRef.current?.files?.[0]; if (!file) throw new Error("Select a resume file."); const body = new FormData(); body.append("file", file); if (displayName) body.append("displayName", displayName); return api.post("/resumes", body)}, onSuccess: () => {setDisplayName(""); if (fileRef.current) fileRef.current.value = ""; void queryClient.invalidateQueries({queryKey: ["resumes"]})}}); return <div className="page"><header className="page-header"><div><span className="eyebrow">Resume workspace</span><h1>Your resumes</h1><p>Upload a readable PDF or DOCX file up to 5 MB.</p></div></header><section className="upload-card"><FileUp size={28}/><div><h2>Upload a resume</h2><p>We extract text, detect skills and show the evidence before analysis.</p></div><input value={displayName} onChange={(e) => setDisplayName(e.target.value)} placeholder="Display name (optional)"/><input ref={fileRef} type="file" accept=".pdf,.docx"/><button className="primary-button" onClick={() => upload.mutate()} disabled={upload.isPending}>{upload.isPending ? "Uploading…" : "Upload"}</button>{upload.error && <div className="error-box">{errorMessage(upload.error)}</div>}</section><section className="content-card"><div className="section-heading"><h2>Uploaded resumes</h2>{resumes.isFetching && <RefreshCw className="spin" size={18}/>}</div><div className="resume-grid">{resumes.data?.map((resume) => <Link to={`/app/resumes/${resume.id}`} className="resume-card" key={resume.id}><div><span className={`status ${resume.status.toLowerCase()}`}>{resume.status}</span>{resume.isActive && <span className="status active-status">ACTIVE</span>}</div><h3>{resume.displayName}</h3><p>{resume.originalFilename}</p><small>{resume.contentRevision > 0 && resume.contentRevision === resume.confirmedRevision ? "Confirmed for analysis" : resume.status === "PROCESSED" ? "Needs review" : resume.processingError ?? "Processing"}</small></Link>)}</div></section></div>}
